<div class="form-article">
  <%= form_for(@article, html: { multipart: true }) do |f| %>
    <% if @article.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

        <ul>
        <% @article.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group col-sm-6">
      <%= label_tag(:title, "Titulo") %>
      <%= f.text_field :title, class: "form-control", required: "required" %>
    </div>
    <div class="form-group col-sm-6">
      <%= label_tag(:subtitle, "Subtitulo") %>
      <%= f.text_field :subtitle, class: "form-control" %>
    </div>
    <div class="form-group col-sm-6">
      <%= label_tag(:date, "Fecha") %>
      <%= f.date_field :date, class: "form-control", required: "required" %>
    </div>
    <!-- <div class="form-group col-sm-6">
      <%= label_tag(:photo, "Foto") %>
      <%= f.file_field :photo, class: "form-control" %>
    </div> -->
    <div class="form-group col-sm-12">
      <%= label_tag(:content, "Cuerpo del artículo") %>
      <%= f.text_area :content, class: "form-control", size: "60x12", required: "required" %>
    </div>

    <div class="clearfix"></div>

    <div class="form-actions">
      <%= f.submit "Guardar", class: "btn-form" %>
      <%= link_to "Volver", articles_path, class: "btn btn-form-blue", data: { no_turbolink: true } %>
    </div>
  <% end %>
</div>
