<%= content_for :title do 'Prensa - Comic Con Colombia 2015' end %>

<header id="press">
  <%= render :partial => 'horizontal_header' %>
</header>

<section id="blog">

  <div class="container">

    <div class="col-xs-12 col-md-10 col-md-offset-2">
      <%= will_paginate @articles, class: 'digg_pagination', previous_label: 'Anterior', next_label: 'Siguiente', data: { no_turbolink: true } %>
    </div>

    <% if @articles.empty? %>

      <h2>Aún no hay entradas</h2>

    <% else %>

      <% @articles.each do |a| %>
        <div class="row">

          <div class="col-xs-12 col-md-2">
            <h2><%=l a.date, :format => :short %></h3>
          </div>

          <div class="col-xs-12 col-md-10">
            <h2><%= a.title %></h2>
            <h4><%= a.subtitle %></h4>
            <!-- <%= link_to image_tag(image_path(a.photo.url), class: "img-thumbnail img-responsive"), '/prensa/'+a.slug, data: { no_turbolink: true } if a.photo.exists? %> -->
            <div class="content-each-blog">
              <%= cut_long_text(a.content).gsub(/\n/, '<br>').html_safe %>
            </div>

            <div class="text-right">
              <%= link_to 'Ver más...', '/prensa/'+a.slug, class: "btn btn-form", data: { no_turbolink: true } %>
            </div>
          </div>

        </div>
      <% end %>  

    <% end %>
    
    <div class="col-xs-12 col-md-10 col-md-offset-2">
      <%= will_paginate @articles, class: 'digg_pagination', previous_label: 'Anterior', next_label: 'Siguiente' %>
    </div>

  </div><!-- /.container -->

</section>